# <App Name> Config

> App identity and conventions. Filled in by `/new-app`; edit as the app matures.

## Title & Scope

- **Title**: <App Name>
- **Slug**: <app-slug>
- **Scope**: one paragraph — what this app is, for whom, and what it is NOT.

## Conventions

- **Confidence**: mark compiled articles and PRDs `confidence: high|medium|low` based on source quality. `high` = primary sources/multiple corroboration; `medium` = single source or inferred; `low` = assumption/unverified.
- **Dual-linking**: every cross-reference uses `[[slug|Name]]` ([Name](../knowledge/topics/slug.md)).
- **Tags**: lowercase, hyphenated; specific over general.
- **Naming**: raw sources `YYYY-MM-DD-slug.md`; knowledge `slug.md`; PRDs `prds/YYYY-MM-DD-slug.md`.

## Domains

| Domain | Owns | Interaction rules |
|--------|------|-------------------|
| _none yet_ | | |

## Tech Stack / Architecture

- _High-level architecture, key technologies, external services — enrich as decided._